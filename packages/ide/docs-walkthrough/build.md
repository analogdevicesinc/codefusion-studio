<!-- markdownlint-disable -->

## Build your application

[Build](command:cfs.runBuildTask "(CFS) Run Build Task") your application to compile the code generated by the System Planner.

Ready to run on hardware? Connect your board, install the drivers, start debugging, and explore the [debugging tools](https://developer.analog.com/docs/codefusion-studio/latest/user-guide/debugging/).