# Mock MSDK Makefile for the CM4 side of a dual-core project
# MAXIM_PATH marker triggers detectsWorkspace()

MAXIM_PATH ?= /mock/msdk/path

TARGET ?= MAX32690
BOARD ?= EvKit_V1
PROJECT ?= max32690_demo

include $(MAXIM_PATH)/Libraries/CMSIS/Device/Maxim/GCC/gcc.mk
